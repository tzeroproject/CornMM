[build]
builder = "NIXPACKS"

[deploy]
# Railway runs only the Express API. The React/Vite frontend is deployed on Cloudflare Pages.
startCommand = "npm run build:server && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
